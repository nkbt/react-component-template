machine:
  node:
    version: v6.1.0

dependencies:
  pre:
    - npm install -g yarn
  override:
    - yarn install

test:
  override:
    - npm start ci.lint
    - npm start ci.test
    - npm start ci.cov
    - npm start ci.e2e

  post:
    - npm start ci.codecov
